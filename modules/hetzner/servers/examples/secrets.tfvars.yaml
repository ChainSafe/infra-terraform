# ---------------------------------------------------------------------------------------------------------------------
# Authentication to CloudFlare, HCloud
#
# Example:
# secrets = {
#   hcloud_token = "SecretSecret"
#   cf_api_token = "SecretSecret"
# }
# ---------------------------------------------------------------------------------------------------------------------
secrets:
  hcloud_token:
  cf_api_token:
